name: weather_icons
description: Flutter library for using erikflowers/weather-icons. An icon pack with over 200 weather themed icons.
version: 0.0.0 # Handled by semantic-release
email: jordon.dehoog@gmail.com
repository: https://github.com/worldturtlemedia/weather_icons
homepage: https://worldturtlemedia.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  fonts:
    - family: WeatherIcons
      fonts:
        - asset: lib/fonts/weathericons-regular-webfont.ttf
          weight: 400
